<h1>add your </h1>
<div class="container">
  <div class="row">
    <div class="col-md-offset-3 col-md-6">

      <% if @dose.errors.any? %>
        <h2>HOUSTON We a problem.</h2>
        <ul>

          <% @dose.errors.full_messages.each do |error| %>
          <li><%= error %></li>
          <% end %>
        </ul>

      <% end %>

      <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.error_notification %>
      <%= f.input :ingredient_id, collection: Ingredient.order(:name) %>
      <%= f.input :description %>


      <%= f.button :submit %>
      <% end %>

    </div>
  </div>
</div>
